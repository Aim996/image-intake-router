# 图片识别路由器后续迭代计划

## 已交付：3.0.0 聚焦识图、预览与确认

3.0.0 已交付 `image-intake-router.v3`：每张图一次初次真实视觉识别、遗漏驱动的最多一次补充识读、清洗文字、一个统一事实集、可见字段审计、按 `【入账内容】` / `【入库内容】` / `【需要注意】` 排列的详细预览，以及后续确认后的最多一次 OpenClaw 交接。`只记账` 和 `只入库` 可限定交接范围。

已交付边界同样明确：初始图片回合零交接；隐藏、遮挡或不可读内容披露而不猜测；可靠可见内容仍可用。路由器不修改下游项目，不拥有下游执行，不迁移、修改或删除下游数据，也不要求下游仓库或 API 变化。

## 未来候选：识别质量与可观测性

可在不增加识读轮次的前提下，评估更清晰的遗漏说明、统一事实冲突提示和经授权的脱敏 UAT 证据。候选项尚未交付，不能改变“初次识读 + 最多一次补充识读”、零猜测和初始零交接约束。

## 未来候选：批次与确认体验

可研究多订单批次的明确分组提示和更清晰的确认范围展示，但不得依据相似商品猜测归属，不得静默扩大 `只记账` / `只入库` 范围，也不得产生重复交接。

## 永不归属路由器的兼容工作

任何未来下游兼容性都由 OpenClaw 的 Skill 发现/调用能力或相应下游 Skill 负责。不得在路由器内重新建立私有 adapter manifest、固定 endpoint/端口、preflight/execute/status 协议、私有 payload 映射、版本协商或下游数据库操作。
