# 图片识别路由器 2.0.0

## 产品定位

`image-intake-router` 2.0.0 是原始图片的统一入口。它把支付截图、小票、订单页、营养标签、食品包装、买菜截图和餐食照片进行**一次**视觉事实识别，再从同一份规范化事实分别生成随手账与食序管家的业务预览。默认展示两份预览，不在展示阶段写入业务系统。

用户确认最近一次完整预览后，路由器才按确认范围提交：确认、可以或就这样提交两个可执行域；只记账或只入库只提交对应域。每张收据或订单至多生成一笔支出，食品库存保持逐项处理。

## 目录与安装

```text
image-intake-router-2.0.0/
├── 项目说明.md
├── 后续迭代计划.md
├── 约束文档.md
└── image-intake-router/
    ├── SKILL.md
    ├── references/
    └── templates/
```

本目录是交付前的暂存副本。验收后的新版本安装目录为 `E:\codx\图片识别sk\image-intake-router-2.0.0`；该目录必须作为新目录创建，不能覆盖已有目标。当前版本不自动复制、安装、启用或部署到正在运行的 OpenClaw。

## 兼容与发布

旧版 `food-image-intake` 及其发布目录保持原样。本版本启用时，旧版原始图片入口必须停用；`food-image-intake` 和 `image-intake-router` 不允许同时激活，以避免同一图片被两个 Skill 竞争或重复识别。
