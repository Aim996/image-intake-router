# 图片识别路由器不可变约束

1. **真实视觉门禁：** 每个附件必须实际进入图片能力且恰好一次；附件名、描述或用户转述不能填充视觉事实。任何附件未执行、失败或不可用时，整体失败关闭，不预览、不写入。
2. **事实可追溯：** 每个识别、推导或计算字段都保存来源、置信度和是否由计算得出；不把未知值伪装为零或空事实。
3. **图片完整性：** 折叠、遮挡和未展示区域必须如实报告可见与隐藏数量；禁止猜测隐藏商品、规格、金额或退款。
4. **单次识别、双域复用：** 统一事实只从一次视觉运行生成。随手账和食序管家适配器只能消费事实，不能再次读取原图。
5. **一次业务确认：** 初始图片回合零业务写入。仅对最新业务摘要的一次后续确认可执行；内部参数修复不得再次确认或产生重复写入。
6. **幂等与恢复：** 每次执行使用稳定调用 ID。状态不确定时先查询下游状态；禁止盲目重试、自动回滚已成功域或凭猜测补写。
7. **本地数据与隐私：** 路由器不持久化原图、路径、base64、完整 OCR、凭据、账户或业务数据库。下游数据始终保留在用户设备的下游 Skill 范围内。
8. **适配边界：** 随手账只接收确认后的公开标量明细；食序管家只接收确认、可靠且食品相关的事实。无法保留业务含义时，该业务域失败关闭。
9. **下游所有权：** 路由器只发布统一事实和适配契约。下游接口、数据库、迁移和适配器由下游项目或独立集成项目维护；未经下游维护者明确授权，路由器不得修改下游仓库、私有接口或数据结构。
10. **公开接口优先：** 新软件按 [外部软件适配接口规范](适配接口规范.md) 提供能力声明、只读预检、幂等执行与状态查询。没有兼容适配器时，该业务域不可执行，不能靠猜测、直写数据库或静默降级绕过。
