# 图片识别路由器后续迭代计划

## 2.0.1：真实图片 UAT 证据

- **进入条件：** 2.0.0 的静态契约、Schema 解析和发布前审计已通过，且具备经授权、已脱敏的真实图片测试样本。
- **交付物：** 支付截图、小票、订单、营养标签、包装和餐食照片的 UAT 记录；每例记录单次识别、双预览、确认范围与实际结果。
- **验证：** 对每例确认确认前零写入；确认后订单至多一笔支出；食品与非食品分流正确；重复确认不会再次写入。
- **不包含：** 不接入真实用户数据库，不保存原图或完整 OCR，也不改变下游工具公开 Schema。

## 2.1.0：多订单与批量图片分离

- **进入条件：** 2.0.1 已证明单订单和重叠截图在真实 UAT 中稳定，且已收集明确的多订单混图失败样本。
- **交付物：** 同批图片中的订单边界识别规则、独立事实集合和按订单生成的预览说明。
- **验证：** 同批两笔订单不会混合金额、商品或确认范围；每个订单仍只生成一笔支出；重叠行只在所属订单内去重。
- **不包含：** 不根据相似商品名猜测跨订单归属，不自动合并用户未确认的订单，也不执行跨数据库原子事务。

## 2.2.0：可选确定性附件指纹

- **进入条件：** 2.1.0 已定义稳定的批次边界和保留策略，并完成隐私与碰撞风险评审。
- **交付物：** 可选、可关闭的确定性附件指纹规范，用于在允许的会话范围内识别同一附件的重复提交。
- **验证：** 相同附件在定义范围内得到相同指纹；不同附件不会因弱特征误合并；关闭功能时不产生指纹。
- **不包含：** 不存储原图、路径、base64、完整 OCR 或账户标识；不使用感知哈希推断相似图片；不把指纹当作自动写入授权。
