# 图片识别路由器后续迭代计划

## 已交付：3.1.0 实用字段与紧凑确认

3.1.0 已交付 `image-intake-router.v3.1`：每张图一次初次真实视觉识别、可见字段遗漏驱动的最多一次补充识读、商品简化名、真实实付、可靠生产日期、退款的临时有效性判断，以及按 `【入账】` / `【入库】` / `【需确认】` 排列的紧凑预览。

初始图片回合零交接；后续确认最多一次；`只记账` 和 `只入库` 可限定范围。路由器不修改下游项目，不拥有下游执行，不迁移、修改或删除下游数据。

## 已保留：3.0.0 与 2.1.0 回滚

v3.0.0 和 v2.1.0 的不可变 tag、Release 与资产继续保留。3.1.0 的安装/回滚文档必须一直包含固定资产和 checksum，不能用移动的 main 分支冒充发布包。

## 未来候选：识别质量

可在不增加识读轮次的前提下评估更多零售平台的商品简化名规则、生产日期标签变体和脱敏 UAT 样本。候选项不得放宽真实像素、最多一次补充识读、零猜测和确认边界。

## 未来候选：批次体验

可研究多订单分组和按订单确认，但不得依据相似名称猜测归属，不得静默扩大确认范围，不得产生重复交接。

## 永不归属路由器

任何下游兼容性由 OpenClaw 或相应下游 Skill 负责。路由器不得重新建立私有 adapter manifest、固定 endpoint/端口、preflight/execute/status 协议、私有 payload、版本协商或下游数据库操作。
