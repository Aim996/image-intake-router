# image-intake-router 识图输出与确认规范

- 适用产品版本：`image-intake-router 3.1.0`
- 适用 Schema：`image-intake-router.v3.1`
- 规范状态：公开输出与确认契约

## 1. 产品职责

路由器让每张图片实际进入视觉能力，整理一个统一事实集，生成紧凑预览，并在用户后续确认后把所选内容一次性交给 OpenClaw。OpenClaw 负责发现与调用下游 Skill。路由器不修改下游项目、私有 API、端口、数据库或数据，不直接执行下游业务。

## 2. 识图与事实

1. 初次识读覆盖每张图片；文件名、附件描述和用户转述不能代替看图。
2. 只有初次结果漏掉像素中明确可见的业务字段时，才可进行最多一次补充识读；生产日期属于可审计字段。
3. 结果汇入一个 canonical fact set。来源冲突保留为未解决问题，不静默覆盖；隐藏、裁切、模糊或不可读内容不猜测。
4. 订单只保留商家、真实交易时间、状态、最终实付和内容完整性。商品只保留全名、简化名、规格、数量、重量/容量、行实付、生产日期、业务状态、类型和可见性。
5. `full_name` 只用于证据和去重；`display_name` 用于预览和交接。相同简化名必须由商品索引和规格区分，不能合并。
6. 退款/取消文字只作临时状态判断，不保存退款金额。部分退款且已收到的商品保留；全额退款、取消、缺货或未收到商品排除。

## 3. 固定紧凑预览

初始含图回合创建零次交接，按下列顺序展示：

```text
识别完成

【入账】
商家｜实付 ¥总额
1. 简化名 规格×数量　¥行实付

【入库】
以上 N 种食品均入库。

生产日期：
简化名 规格｜YYYY-MM-DD

【需确认】
只列需要用户决定的问题。

回复“确认”执行；也可回复“只记账”或“只入库”。
```

商品明细只列一次。仅部分商品可入库时，入库段列出那些商品；没有可靠生产日期时省略整段；没有问题时省略 `【需确认】`。退款、短重、原价、单价、优惠、会员节省、费用拆分、免费/赠品解释和内部字段不进入默认预览。

## 4. 后续确认与范围

- 普通肯定确认：交接可靠入账与入库内容。
- `只记账`：只交接 accounting。
- `只入库`：只交接 inventory。

纠正业务事实后必须生成新预览再确认。内部序列化、单位转换、空字段处理、下游参数映射或技术重试不是新业务决定，不要求再次确认。相同确认最多产生一次 handoff；图片回合和重复确认产生零次新 handoff。

## 5. OpenClaw 边界

确认交接只表达统一事实和用户选择的范围。OpenClaw 拥有下游 Skill 的发现/选择/调用；下游 Skill 拥有校验、执行、存储、幂等、状态、恢复和兼容性。路由器不定义私有 adapter、endpoint、端口、preflight/execute/status 协议、payload 映射或数据库操作。
