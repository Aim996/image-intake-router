# 图片识别路由器 3.0.0

## 产品定位

`image-intake-router` 3.0.0（Schema `image-intake-router.v3`）是 OpenClaw 的图片识别、预览与确认入口。它负责真实视觉识别、可见字段审计、统一事实和详细三段预览；OpenClaw 负责发现与调用下游 Skill，路由器不拥有下游执行。

```text
每张图一次初次视觉识别
  → 如有可见字段遗漏，最多一次补充识读
  → 清洗文本并形成一个统一事实集
  → 【入账内容】→【入库内容】→【需要注意】
  → 初始含图回合零交接
  → 后续明确确认（可选“只记账”或“只入库”）
  → OpenClaw 最多一次下游 Skill 交接
```

## 已交付能力

- 每张图片都必须实际进入视觉能力；文件名、描述或用户转述不能代替像素事实。
- 初次识读后只允许因遗漏进行最多一次补充识读，所有结果合并为一个无冲突的 canonical fact set。
- 预览按固定三段顺序详细展示商家、时间、金额、退款、可见商品名称、规格、数量、重量/容量、价格和限制。
- 初始图片回合只预览、零交接；后续肯定确认最多交接一次，范围短语 `只记账` 与 `只入库` 精确限制交接内容。
- 隐藏、遮挡、裁切、模糊、被阻止或不可读内容会披露而不会猜测；可靠可见内容仍可使用，存在隐藏项时只交接可见内容。

## 所有权与数据边界

OpenClaw 拥有下游 Skill 的发现和调用。路由器不检查、不修改下游项目、私有 API、端口、接口、数据库、数据、重试/状态行为或适配协议，不维护 adapter manifest、私有 payload 或版本协商。下游 Skill 自己负责校验、执行、存储、幂等、状态与恢复。

v3 不迁移、修改或删除下游数据，也不要求下游仓库、API 或数据库做任何变化。详细契约见 [识图输出与确认规范](识图输出与确认规范.md)。

## 安装、更新与回滚

3.0.0 固定资产为 `image-intake-router-3.0.0.tgz` 与 `image-intake-router-3.0.0.tgz.sha256`。在多台设备上都应从固定 GitHub Release 下载、校验 SHA-256，并只安装归档内嵌套 `image-intake-router/` 目录；详见 [安装指南](docs/INSTALL.md) 和 [更新与回滚](docs/UPGRADING.md)。

保留不可变的 [v2.1.0 发布页](https://github.com/Aim996/image-intake-router/releases/tag/v2.1.0)、`image-intake-router-2.1.0.tgz`、`image-intake-router-2.1.0.tgz.sha256`、已验证旧 Skill 目录及配置作为回滚目标。恢复旧版本只替换 Skill 与 OpenClaw 配置，不覆盖或迁移任何下游数据库。
