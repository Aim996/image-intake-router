# 图片识别路由器 3.1.0

## 产品定位

`image-intake-router` 3.1.0（Schema `image-intake-router.v3.1`）是 OpenClaw 的识图、整理、预览与确认入口。它只处理图片和统一业务事实，不修改下游项目，也不直接调用记账或库存程序。

```text
每张图一次初次真实视觉识别
  → 可见业务字段遗漏时，最多一次补充识读
  → 一个统一事实集
  → 商品全名只用于证据/去重，业务内容使用简化名
  → 【入账】→【入库】→【需确认】
  → 初始含图回合零交接
  → 用户后续确认一次
  → OpenClaw 按范围发现并调用下游 Skill
```

## 3.1.0 已交付能力

- 每张图片必须实际进入视觉能力；文件名、附件描述或用户转述不能冒充像素事实。
- 只保留订单最终实付和商品行实付；真实 `¥0.00` 原样保留，不推测赠品、免费或会员权益。
- `full_name` 只留在事实层用于证据与去重；入账、入库和交接使用简洁 `display_name`，例如鲜牛奶、豆浆、果蔬汁。
- 图片明确可见的生产日期进入统一事实、入库内容和确认预览；没有日期时省略整段，不估算。
- 退款或取消文字只用于判断商品是否收到。部分退款但已收到的商品保留；全额退款、取消或未收到商品排除；退款金额不记录、不展示、不交接。
- 商品明细默认只列一次。全部是可入库食品时，入库段用“以上 N 种食品均入库”汇总。
- `【需确认】` 只保留用户能决定的问题，不展示 Schema、工具、内部状态或金额倒推过程。

## 所有权与数据边界

OpenClaw 拥有下游 Skill 的发现和调用。路由器不检查或修改下游项目、私有 API、端口、接口、数据库、数据、重试/状态行为或适配协议，不维护私有 payload 或版本协商。下游 Skill 自己负责校验、执行、存储、幂等、状态与恢复。

3.1.0 不迁移、修改或删除下游数据，也不要求下游仓库/API/数据库做变化。完整公开契约见 [识图输出与确认规范](识图输出与确认规范.md)。

## 安装、更新与回滚

当前固定资产为 `image-intake-router-3.1.0.tgz` 与 `image-intake-router-3.1.0.tgz.sha256`。多台设备可从固定 GitHub Release 下载并校验；源码副本可用 `git pull --ff-only origin main` 更新。正式安装、v3.0.0/v2.1.0 回滚步骤见 [安装指南](docs/INSTALL.md) 与 [更新与回滚](docs/UPGRADING.md)。
