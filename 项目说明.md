# 图片识别路由器 2.0.0

## 产品定位

`image-intake-router` 2.0.0 是原始图片的统一入口。它把支付截图、小票、订单页、营养标签、食品包装、买菜截图和餐食照片进行**一次**视觉事实识别，再从同一份规范化事实分别生成随手账与食序管家的业务预览。默认展示两份预览，不在展示阶段写入业务系统。

用户确认最近一次完整预览后，路由器才按确认范围提交：确认、可以或就这样提交两个可执行域；只记账或只入库只提交对应域。每张收据或订单至多生成一笔支出，食品库存保持逐项处理。

## 架构与约束

```text
图片批次（只识别一次）
        ↓
规范化事实与跨图去重
        ↓
随手账 expense_projection + 食序管家 diet_projection
        ↓
用户确认后执行所选范围
```

- 首个含图片回合只生成预览，业务写入次数必须为零。
- 修改预览会生成新 revision，旧 revision 立即失效。
- 执行前消费预览；重复确认不会产生新的业务写入。
- 不保存原图、完整 OCR、支付账户、凭据或本地业务数据库。

## 安装与发布

GitHub Release 是公开安装的默认入口。请从发布页取得固定版本 2.0.0，核验 SHA-256 后再将它安装到新的 OpenClaw Skill 目录。具体步骤见 [安装指南](docs/INSTALL.md)，升级和回滚见 [更新与回滚指南](docs/UPGRADING.md)。

旧版 `food-image-intake` 保持原样，但不能与 `image-intake-router` 同时激活，以避免同一图片被两个 Skill 竞争或重复识别。当前版本不会自动复制、安装、启用或部署到正在运行的 OpenClaw。
