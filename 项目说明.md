# 图片识别路由器 2.1.0

## 产品定位

`image-intake-router` 2.1.0（协议 `image-intake-router.v2.1`）是 OpenClaw 中支付截图、小票、订单页、食品包装和餐食图片的统一入口。它只执行一次真实视觉识别，并把结果整理为可追溯的统一事实；随后分别向随手账和食序管家提供适配后的业务数据，而不是让两个 Skill 再读一遍图片。

```text
图片批次（每个附件恰好一次真实视觉能力）
                    ↓
统一事实（字段来源、置信度、计算标记、完整性）
                    ↓
随手账标量明细适配器 ──┬── 食序管家事实适配器
                    ↓
简洁业务预览 → 一次业务确认 → 独立执行与可查询恢复
```

## 当前能力与边界

- 统一事实保留商品全名与简名、规格、数量、标称/实际/计价重量或容量、单项原价/单价/实付/退款、商家、订单时间、订单状态和图片完整性。
- 随手账收到商品名、规格、数量、单项实付及退款等标量明细；完整明细保存于账本，聊天默认只显示简洁摘要。
- 食序管家读取同一事实，不触发第二次图片识别；仅可靠识别、确认且属于食品的项目可入库。
- 初始含图回合只识别与预览，业务写入为零。用户只确认一次业务内容；内部单位转换、校验或同一幂等键的适配器修复不重复确认、不重复写入。
- 每个附件必须有真实视觉结果。图片能力未执行、失败或不可用时失败关闭：不生成预览、不写入。折叠、遮挡或未展示商品必须说明可见数与隐藏数，绝不猜测。
- 不保存原图、图片路径、base64、完整 OCR、账户、凭据或路由器本地业务数据库。

不支持把附件文件名、描述文字或猜测当成图像事实；不处理收入、余额、转账、借贷、投资、资产，也不把退款在识别阶段丢弃。

## 外部软件接入

路由器只发布稳定的统一事实和适配契约，不拥有也不直接修改下游软件的代码、公开接口或数据库。新的记账、库存、饮食或其他软件应在自己的项目中，或在独立集成项目中实现适配器，再由路由器调用其明确公开的能力。

完整的能力声明、只读预检、确认后执行、幂等、状态查询、版本兼容和权限边界见 [外部软件适配接口规范](适配接口规范.md)。当前内置的随手账和食序管家投影属于现有兼容实现，不代表路由器有权改动这两个项目。

## 安装、更新与回滚

发布时从 2.1.0 的固定 Release 取得 `image-intake-router-2.1.0.tgz` 与对应 SHA-256；只安装归档中的嵌套 `image-intake-router/` 目录。详细方法见 [安装指南](docs/INSTALL.md)。升级采取并行目录与配置备份策略，见 [更新与回滚](docs/UPGRADING.md)。

不可变的 [v2.0.1 发布页](https://github.com/Aim996/image-intake-router/releases/tag/v2.0.1) 和其固定资产 `image-intake-router-2.0.1.tgz` 是回滚目标。回滚只恢复已验证的旧 Skill 目录和 OpenClaw 配置，不覆盖下游数据库。
