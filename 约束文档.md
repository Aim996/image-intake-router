# 图片识别路由器不可变约束

1. **单次识别：** 一张图片或同一批图片只进行一次视觉解释；随手账和食序管家投影只能读取同一份规范化事实，不能再次读取原图。
2. **确认前零写入：** 默认先展示记账与入库两份预览。没有对最近完整预览的确认，不调用任何业务写入。
3. **一次消费：** 预览状态为 `awaiting_confirmation` 时才可确认；进入执行后不可再确认，执行结束即为 `consumed`。已消费预览不能再次执行。
4. **范围与修改：** 确认、可以、就这样执行全部可执行投影；只记账和只入库缩小范围。金额、分类、商品、规格、数量或范围修改后，旧预览失效，必须重新预览并确认。
5. **一单一笔：** 一张票据或一笔订单至多创建一笔支出；已识别的实际购买商品名称写入账目备注，而不是拆成多笔账。
6. **仅食品入库：** 只有已确认且可可靠识别的食品候选可进入食序管家。非食品、费用、广告、取消、退款、缺货和未送达项目不得入库。
7. **隐私边界：** 不保留、导出或写入原图、图片路径、base64、完整 OCR、支付账户、凭据、内部句柄或预览标识。
8. **故障边界：** 两个下游数据库独立提交，必须如实报告部分成功。结果不确定时先按下游契约查询状态，禁止盲目重试或自动回滚已经成功的业务域。
9. **发布边界：** 旧版 `food-image-intake` 保留不改；不自动部署到真实 OpenClaw；新旧两个原始图片 Skill 不得同时激活。
10. **工具与数据库边界：** 只能通过既有公开工具 Schema 调用；不得传入未声明参数，也不得直接编辑随手账或食序管家的 SQLite 数据库。
