# image-intake-router 外部软件适配接口规范

- 规范版本：`image-intake-router.adapter.v1.0`
- 适用路由协议：`image-intake-router.v2.1`
- 适用产品版本：`image-intake-router 2.1.x`
- 规范状态：稳定边界，新增适配器按本规范接入

## 1. 目的

本规范说明记账、库存、饮食管理或其他软件如何接收 `image-intake-router` 的统一图片事实。核心原则是：**路由器发布稳定事实契约，下游软件实现并维护自己的适配器。**

路由器不得为了接入某个软件而直接修改该软件的源代码、公开接口、数据库结构或历史数据。确实需要下游变更时，应由下游维护者在下游项目内实现，或者建立独立的集成项目；路由器只调用对方明确公开并声明兼容的接口。

```text
图片附件
   ↓ 仅一次真实视觉识别
image-intake-router 统一事实
   ↓ 适配契约（本规范）
下游维护的适配器 / 独立集成适配器
   ↓ 下游公开接口
下游软件与其本地数据
```

## 2. 所有权与禁止事项

| 组件 | 负责内容 | 不负责内容 |
| --- | --- | --- |
| 路由器 | 一次视觉识别、统一事实、证据与置信度、图片完整性、业务摘要、一次确认、稳定操作 ID、统一执行结果 | 下游数据库、下游内部模型、下游迁移脚本、私有参数、未公开接口 |
| 下游适配器 | 能力声明、事实映射、单位和格式转换、公开调用、幂等写入、状态查询、技术错误归一化 | 重新识别图片、修改统一事实、代替用户确认业务变化 |
| 下游软件 | 自己的 API/Tool Schema、业务校验、事务、存储、备份、升级与兼容策略 | 要求路由器直接写数据库或猜测私有字段 |

以下行为明确禁止：

1. 路由器或适配器再次读取原图、文件路径、base64 或完整 OCR。
2. 路由器直接编辑下游仓库、数据库或配置来“强行兼容”。
3. 适配器把未知字段编造成零、空字符串、默认日期或默认商品。
4. 下游不支持结构化明细时，路由器静默丢弃明细并降级成备注。
5. 因技术字段、单位或参数顺序调整而要求用户再次确认。
6. 写入结果不确定时盲目重试。

## 3. 接入方式与端口

本规范是**传输无关**的，不要求固定网络端口。推荐优先级如下：

1. OpenClaw 公开 Tool/Skill 调用；
2. 本机进程间调用或命令行适配器；
3. 下游已经公开并维护的本地 HTTP 接口；
4. 下游已经公开并维护的远程 HTTPS 接口。

若使用 HTTP，监听地址、端口、认证与证书均由下游适配器声明和维护，不能硬编码进路由器核心。路由器配置只保存适配器标识和必要的调用位置；凭据必须使用 OpenClaw 或运行环境的秘密配置，不能进入统一事实、预览、日志或 Git 仓库。

当前 `image-intake-router 2.1.x` 内置费用和食序投影，但尚不提供第三方适配器的自动发现。第三方适配器在 2.1.x 中需要静态登记；静态登记只建立调用关系，不授权修改下游项目。后续路由版本可以实现自动发现，但不得改变本规范的所有权边界。

## 4. 下游必须提供的四个逻辑操作

实际 Tool 名或 HTTP 路径可以不同，但语义必须等价。

### 4.1 `capabilities`：能力声明

- 只读，不产生业务写入。
- 返回适配器身份、支持的路由协议、业务域、明细模式、幂等与状态查询能力。
- 路由器在生成可执行预览前检查兼容性；不兼容时该业务域失败关闭。

### 4.2 `preflight`：写入前校验

- 只读，不产生业务写入。
- 检查统一事实能否无损映射到下游公开接口。
- 返回 `ready` 或 `not_executable`，并给出面向人的问题列表。
- 可以完成确定性的单位、枚举和日期格式转换，但不得改变业务含义。

### 4.3 `execute`：确认后执行

- 仅在用户确认最新业务摘要后调用。
- 必须接收稳定 `operation_id` 作为幂等键。
- 同一 `operation_id` 的重复调用必须返回同一已知结果，不能重复写入。
- 只能调用下游公开接口，不能绕过接口直接写数据库。

### 4.4 `status`：不确定结果查询

- 只读，不产生新业务写入。
- 使用 `operation_id` 查询此前调用是否已经提交。
- 结果仍无法确定时返回 `indeterminate`；不得自动重放 `execute`。

对于允许写入的适配器，`idempotency` 与 `status_query` 是强制能力。缺少任一能力时，只能作为预览适配器，不能被路由器标记为可执行。

## 5. 能力声明格式

下游项目应在其仓库中维护一份机器可读声明，例如 `image-intake-router-adapter.json`：

```json
{
  "adapter_contract": "image-intake-router.adapter.v1.0",
  "adapter_id": "com.example.personal-inventory",
  "adapter_version": "1.0.0",
  "accepted_router_schemas": ["image-intake-router.v2.1"],
  "scopes": ["inventory"],
  "modes": ["structured_items"],
  "operations": ["capabilities", "preflight", "execute", "status"],
  "capabilities": {
    "idempotency": true,
    "status_query": true,
    "structured_items": true,
    "partial_visible_items": true,
    "refund_facts": "preserve",
    "max_items": 100
  }
}
```

字段要求：

- `adapter_id`：反向域名形式的稳定唯一标识；升级时不得随意改变。
- `adapter_version`：适配器自身的语义化版本，不等于下游产品版本。
- `accepted_router_schemas`：明确列出已通过测试的路由协议。
- `scopes`：适配器支持的业务域，例如 `expense`、`inventory`、`diet`。
- `modes`：`structured_items` 表示接收商品级结构化事实；`summary_only` 表示只能接收摘要。
- `partial_visible_items`：是否允许在明确标注图片不完整时，只处理可见商品。
- `refund_facts`：必须为 `preserve` 或 `unsupported`；不得在适配层直接丢弃退款事实。
- `max_items`：单次请求可接受的最大可见商品数。

仅支持 `summary_only` 的下游必须显式声明。路由器只有在用户事先接受该业务域会损失结构化明细时才可启用；不能把 `structured_items` 静默降级为备注。

## 6. 路由器提供的统一事实

路由器先使用 `image-intake-router/templates/image-intake-router.schema.json` 校验完整文档。第三方适配器只消费以下公共路径，不接收原图或路由器私有状态：

| JSON 路径 | 含义 |
| --- | --- |
| `/schema_version` | 路由事实协议，当前为 `image-intake-router.v2.1` |
| `/source/image_count` | 本批图片数量 |
| `/recognition_run/status` | `succeeded`、`partial`、`failed` 或 `not_executed` |
| `/recognition_run/attachments` | 每张图片是否真正进入视觉能力及其完整性限制 |
| `/facts/order` | 商家、时间、订单状态、总额、优惠、费用、实付、退款和完整性计数 |
| `/facts/products` | 可见商品的名称、规格、数量、重量/容量、价格、退款、状态与证据 |
| `/facts/unresolved_issues` | 尚未解决的事实问题 |
| `/quality` | 视觉能力、事实集状态和质量问题 |

每个事实值是带来源的包装对象，而不是裸值。适配器必须保留这些语义：

```json
{
  "value": 11.78,
  "currency": "CNY",
  "confidence": 0.98,
  "calculated": false,
  "evidence": [
    {
      "source": "visible_label",
      "value": "实付 ¥11.78",
      "location": "第 1 张图，第 1 个可见商品"
    }
  ]
}
```

已知值必须有证据；未知值保持 `null`。`partial` 只表示图片确实经过视觉识别但存在折叠、裁切、遮挡或模糊等限制。`failed` 或 `not_executed` 时不能进入适配器写入流程。

## 7. 适配请求信封

路由器调用 `preflight` 或 `execute` 时使用同一业务信封；`execute` 额外要求已确认状态。以下 JSON 只展示信封形状，`facts.order` 与 `facts.products` 在真实请求中必须替换为通过路由 Schema 校验的完整事实，不能发送示例中的空对象或空数组冒充有效订单：

```json
{
  "adapter_contract": "image-intake-router.adapter.v1.0",
  "operation": "preflight",
  "operation_id": "stable-opaque-id",
  "scope": "inventory",
  "router_schema_version": "image-intake-router.v2.1",
  "business_digest": "sha256:opaque-digest",
  "confirmed": false,
  "recognition": {
    "status": "partial",
    "attachment_count": 1,
    "limitations": ["页面底部还有 2 种商品未展开"]
  },
  "facts": {
    "order": {},
    "products": [],
    "unresolved_issues": []
  }
}
```

要求：

- `operation_id`：由路由器生成的不透明稳定 ID；同一业务写入始终复用。
- `business_digest`：用户确认内容的稳定摘要；适配器不能自行改写。
- `confirmed`：`preflight` 必须为 `false`；`execute` 必须为 `true`。
- `facts`：来自已校验统一事实的原样子树，不得混入下游技术字段。
- `recognition.limitations`：由识别运行和质量问题归纳，不能改写成“完整”。

业务摘要至少覆盖实付、商家、业务时间、商品名称、数量、规格、重量/容量、单项价格、退款、选中业务域、可见/隐藏数量和图片完整性。任何这些内容发生变化，都必须生成新摘要并重新让用户确认。

## 8. 映射规则

适配器只能做单向、确定性的技术映射：

- 可以把 `kg` 精确换算成 `g`、把 `L` 精确换算成 `ml`；
- 可以把展示单位映射为下游已公开的枚举，同时保留原始展示事实；
- 可以按下游公开 Schema 省略未知可选字段；
- 可以调整字段顺序、参数名称和合法的日期格式；
- 不得修改商品身份、数量业务含义、实付、单项价格、退款或图片完整性；
- 不得把订单总额冒充单项价格；
- 不得把已知隐藏商品猜成具体商品；
- 不得把退款直接转换为收入、负支出或第二笔业务写入，除非下游业务明确支持且用户另行确认。

记账型适配器通常应将一张票据映射为一笔支出，并把所有可见商品保存为结构化明细；聊天摘要可以缩短，数据库明细不能跟着截断。库存或饮食型适配器通常按符合条件的可见商品逐项写入。两者都是映射示例，不授权路由器修改下游的业务模型。

## 9. 标准响应

### `preflight` 响应

```json
{
  "status": "ready",
  "lossless": true,
  "issues": [],
  "normalized_summary": "7 个可见商品可处理；另有 2 个商品未展开"
}
```

`status` 只能是 `ready` 或 `not_executable`。`lossless: false` 时路由器不得自动执行；应说明信息损失并要求业务层决定是否接受新的摘要。

### `execute` / `status` 响应

```json
{
  "status": "written",
  "operation_id": "stable-opaque-id",
  "downstream_reference": "opaque-reference",
  "message": "已写入 7 个可见商品"
}
```

`status` 只能使用下列值：

| 状态 | 含义 | 路由器行为 |
| --- | --- | --- |
| `not_executed` | 尚未开始提交 | 确认后可执行 |
| `written` | 下游明确已经提交 | 永不重放 |
| `failed_before_write` | 下游明确证明未提交 | 可做至多一次不改变摘要的确定性技术修正 |
| `indeterminate` | 可能已提交，但无法确认 | 先查 `status`；仍不确定则停止，不重试 |

`downstream_reference` 必须是不透明、安全、可用于用户本机查询的引用；不能返回访问令牌、数据库连接、绝对隐私路径或内部凭据。

`status` 查询只需要 `adapter_contract`、`adapter_id` 和原 `operation_id`；不得要求重新发送图片，也不得用新的操作 ID 查询旧写入。

## 10. 一次确认与错误恢复

标准流程为：

```text
识别一次 → 校验统一事实 → 适配器 preflight（零写入）
       → 展示各业务域摘要 → 用户确认一次
       → execute → 必要时 status 查询 → 简洁结果
```

用户确认的是业务内容，不是 `piece`、`expires_at`、字段顺序、端口或适配器版本。只要 `business_digest` 不变，确定性的技术修正沿用原确认；若商品、金额、数量、规格、退款、选中域或完整性结论变化，旧确认立即失效。

一个业务域失败不能回滚或重放另一个已经成功的业务域；一个商品失败也不能重放已经成功的同批商品。

## 11. 版本兼容

- `adapter_contract` 的主版本变化表示破坏性变更；次版本只允许增加可选能力；修订版本只允许澄清和修复不改变语义的问题。
- 下游只对 `accepted_router_schemas` 中明确列出的路由协议负责。
- 路由器升级统一事实协议时，必须先保留旧适配路径或把该适配器标记为不兼容，不能猜测调用。
- 下游公开接口变化时，由下游适配器升级 `adapter_version` 并完成兼容测试；路由器核心不跟随修改下游源代码。
- 适配器不兼容时，该业务域显示为不可执行；其他兼容业务域仍可独立预览和执行。

## 12. 下游接入清单

下游软件完成以下项目后，才可声明适配完成：

- [ ] 在下游仓库或独立集成仓库实现适配器，没有修改路由器核心事实。
- [ ] 提供 `capabilities`、`preflight`、`execute`、`status` 四个逻辑操作。
- [ ] 声明并验证 `image-intake-router.v2.1`。
- [ ] `preflight` 和 `status` 被测试为零业务写入。
- [ ] 同一 `operation_id` 重复执行不会重复写入。
- [ ] 能从 `indeterminate` 查询真实提交状态，不能盲重试。
- [ ] 对完整、部分可见、视觉失败、退款、未知字段和超过条目上限分别测试。
- [ ] 结构化商品明细不会因聊天摘要缩短而丢失。
- [ ] 不保存原图、路径、base64、完整 OCR 或凭据。
- [ ] 业务字段变化会触发新确认，纯技术映射不会重复确认。
- [ ] 由下游维护者审核并发布适配器版本。

## 13. 现有内置投影的地位

`image-intake-router 2.1.x` 中的 `expense_projection` 与 `diet_projection` 是现有兼容实现，不是路由器修改随手账或食序管家项目的授权。新增软件应优先按照本规范在自己项目或独立集成项目中实现适配器。

若现有内置投影与下游新版本不再兼容，正确处理是：下游发布新的适配声明和公开接口，路由器把旧适配标记为不可执行，双方通过版本契约升级。不得由路由器直接进入下游项目修改接口或数据库。
