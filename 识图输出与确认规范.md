# image-intake-router 识图输出与确认规范

- 适用产品版本：`image-intake-router 3.0.0`
- 适用 Schema：`image-intake-router.v3`
- 规范状态：公开输出与确认契约

## 1. 产品职责

`image-intake-router` 只负责图片识别入口：让每张图片实际进入视觉能力，整理一个统一事实集，审计可见字段，生成详细预览，并在用户随后确认后把已确认事实一次性交给 OpenClaw。OpenClaw 负责发现并调用合适的下游 Skill。

路由器不拥有下游执行。它不检查或修改下游项目、私有 API、端口、接口、数据库、数据、重试/状态行为或适配协议，也不要求下游仓库或 API 为 v3 做变更。v3 不迁移、修改或删除任何下游数据。

```text
图片附件
  → 初次视觉识别（每张图一次）
  → 必要时最多一次、仅针对遗漏字段的补充识读
  → 清洗文本 + 统一事实集 + 可见字段审计
  → 三段详细预览（零交接）
  → 用户后续确认
  → OpenClaw 发现并调用下游 Skill（最多一次交接）
```

## 2. 识图与统一事实

1. 初次识读必须覆盖每张图片，并以真实像素/媒体内容为证据；文件名、附件描述和用户转述不能代替看图。
2. 若初次识读遗漏了可见的关键字段，可以进行最多一次补充识读。补充识读只能针对已发现的遗漏，不得重新开展无限轮识别。
3. 路由器清理明显的 OCR 噪声，但不能创造图片中不可见的事实。所有输出必须汇入一个统一、规范的 canonical fact set；来源不一致的证据必须保留为未解决冲突并显示警告，绝不静默覆盖。下游预览与交接都复用该事实集。
4. 可见字段应尽可能详细，包括商家、时间、订单状态、总额、优惠、费用、实付、退款，以及每个可见商品的名称、规格、数量、重量/容量、价格和状态。
5. 遮挡、折叠、裁切、模糊、不可读或被界面阻止的内容必须明确披露，不能猜测。可靠的可见内容仍然可用，不因存在隐藏项目而整体阻塞。

## 3. 固定预览顺序

初始含图回合只识别并展示预览，创建零次下游交接。预览必须严格按以下顺序输出：

1. `【入账内容】`：可用于记账的商家、时间、金额、退款和可见商品明细；没有可靠内容时明确写“无”。
2. `【入库内容】`：可用于库存/食品管理的可见商品名称、规格、数量、重量/容量等；没有可靠内容时明确写“无”。
3. `【需要注意】`：隐藏、遮挡、裁切、模糊、不可读、冲突、未知值及其他限制；没有问题时明确写“无”。

预览来自同一个统一事实集。它不是下游执行结果，不得声称已经记账、入库或修改数据库。

## 4. 后续确认与范围

只有用户在后续回合给出明确肯定确认，路由器才可创建一次 OpenClaw 交接。确认范围如下：

- 普通肯定确认：交接预览中可靠的入账与入库内容。
- `只记账`：只交接 `【入账内容】`，不交接入库内容。
- `只入库`：只交接 `【入库内容】`，不交接入账内容。

否定、疑问、修改请求或含糊回复都不是确认。事实或范围改变后必须重新展示最新预览，再等待新的明确确认。相同确认不能产生重复交接；初始图片回合永远不能交接。

## 5. 部分可见与不可读内容

存在隐藏项目时，交接只包含可靠可见内容，并把隐藏数量或限制保留在说明中。路由器不得猜测隐藏商品、规格、数量、金额或退款，也不得因为隐藏项目阻塞可靠可见内容。

若某字段不可读或被遮挡，保持未知并披露原因；不得填成 `0`、空字符串、默认日期或虚构商品。若没有任何可靠可见业务内容，则只说明问题并保持零交接。

## 6. OpenClaw 交接边界

确认后的交接只表达已确认的统一事实和用户选择的范围。OpenClaw 拥有下游 Skill 的发现、选择与调用；下游 Skill 拥有自己的业务校验、执行、存储、幂等、状态、恢复和兼容性。

路由器明确不做以下工作：

- 不定义或协商 adapter manifest、adapter version 或 capabilities；
- 不固定逻辑 endpoint、HTTP 路径、端口、认证或传输协议；
- 不运行下游 preflight、execute 或 status 协议；
- 不定义下游响应 Schema、幂等键、重试或恢复状态机；
- 不维护私有 payload 映射，不把事实改写成下游私有字段；
- 不进入下游仓库，不读取私有接口或数据库，不迁移、修改、删除或补写下游数据。

未来新增下游兼容性时，应由 OpenClaw 或相应下游 Skill 处理，不能重新引入路由器拥有的私有适配器协议。
