# 图片识别路由器不可变约束

1. **必须看真实像素：** 每张附件实际进入视觉能力；文件名、附件描述和用户转述不能填充图片事实。
2. **有限识读：** 每张图一次初次识读；只有可见业务字段遗漏时允许最多一次补充识读，禁止第三次识读。
3. **统一事实：** 清洗文字、字段、来源、置信度和限制合并为一个 canonical fact set；两个业务视图不得重新看图。
4. **实付优先：** 只记录唯一最终实付和商品行实付；不倒推优惠，不用退款抵扣实付，不解释零元原因。
5. **退款不落事实：** 退款/取消文字只用于判断状态；部分退款已收商品保留，全额退款/取消/未收到商品排除；退款金额不显示、不记录、不交接。
6. **名称分层：** `full_name` 只用于证据/去重；`display_name` 用于入账、入库、预览和 handoff；同名不同规格不得合并。
7. **生产日期不猜测：** 只记录可靠可见的 `production_date`；不得用送达时间、交易时间、保质期、到期日或食品常识替代。
8. **紧凑预览：** 初始回合按 `【入账】`、`【入库】`、可选 `【需确认】` 输出；商品列表默认不重复，图片回合零交接。
9. **后续一次确认：** `确认` 选择全部可靠内容，`只记账`/`只入库` 限定范围；重复确认零新增 handoff。
10. **下游隔离：** OpenClaw 负责下游 Skill 发现/调用。路由器不修改下游项目、私有 API、端口、接口、数据库或数据，不拥有适配、执行、状态或重试协议。
11. **无数据迁移：** 3.1.0 不迁移、修改或删除下游数据；安装和回滚只替换 Skill 与 OpenClaw 配置。

公开契约见 [识图输出与确认规范](识图输出与确认规范.md)。
