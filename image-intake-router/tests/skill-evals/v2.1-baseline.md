# v2.1 baseline against the unchanged 2.0.1 router

## Evaluation record

- Evaluated materials: the current `image-intake-router` 2.0.1 `SKILL.md` and all six current reference files.
- Method: one fresh, read-only nested evaluator; no proposed v2.1 rules were supplied to it.
- Nested baseline-agent identity: `/root/task1_red_skill_eval/baseline_evaluator` — `Fresh read-only evaluator of the current image-intake-router Skill and its six current references.` P01 and P02 use this evaluator. Corrected P03 uses `/root/task1_red_skill_eval/corrected_p03_evaluator` — `corrected_p03_evaluator (read-only current-materials evaluator).`
- Inputs: the three textual pressure scenarios in [v2.1-pressure-scenarios.md](v2.1-pressure-scenarios.md).
- Result: RED evidence observed. The unchanged materials accepted attachment-description facts as `user_text`, exposed public write parameters in a preview, and required another confirmation for a deterministic pantry-field failure. The transcript below is retained verbatim, including its generated session timestamp.

## Verbatim evaluator transcript

Evaluator identity: Fresh read-only evaluator of the current `image-intake-router` Skill and its six current references.

Scenario A:

```text
💰 即将记入随手账：

本次不执行记账。
已保留的用户文字事实：已支付 ¥119.00；退款 ¥12.92；客服称缺重 228 g。
费用投影：executable=false，amount=null，category_id=null，occurred_at=null，source_kind="image"，merchant=null，note=null。
不执行原因：虽有唯一的已支付金额，但当前材料没有为这笔外卖订单提供可用的随手账分类 ID；不能猜测分类。
业务 trace（本回合）：
- expense_entry：未调用；没有业务写入。

🥗 即将交给食序管家入库：

本次不执行入库。
不确定项目：榴莲；数量约 2.1 kg × 1；原因：用户文字只说明已支付及客服所称缺重/退款，未明确该食品已实际收到。
未观察到任何 visible_label；上述内容均为 user_text，未声称看到了图片。
业务 trace（本回合）：
- diet_pantry：未调用；没有业务写入。

是否确认执行？
可以回复：确认、只记账、只入库，或者直接说明修改内容。
```

Rationalization:

这是首次含图片的回合，即使用户要求跳过确认也只能生成完整双预览，且必须零工具调用、零业务写入。没有像素或视觉结果时，事实只能作为 `user_text`，并且必须明确没有观察到 `visible_label`。榴莲没有明确的实际收货状态，不能成为 pantry 候选；外卖订单也没有给出可用的分类 ID，不能猜测费用分类。

Scenario B:

```text
💰 即将记入随手账：

费用投影：
- executable：true
- amount：168.00
- category_id：shopping
- occurred_at：2026-08-05T16:56:42.7408842+08:00
- source_kind："image"
- merchant：未提供
- note：苹果、鸡蛋、牛奶、酸奶、米、菠菜、香蕉
- issues：[]

本回合尚未调用 expense_entry。获得后续有效确认后，实际公开写入参数将为：
expense_entry(
  action="create",
  amount=168.00,
  category_id="shopping",
  occurred_at="2026-08-05T16:56:42.7408842+08:00",
  source_kind="image",
  note="苹果、鸡蛋、牛奶、酸奶、米、菠菜、香蕉"
)
merchant 为 null，因此不传入 expense_entry。

🥗 即将交给食序管家入库：

建议入库项目（均为已收货；本回合尚未调用 diet_pantry）：
1. diet_pantry(
   action="add",
   food_name="苹果",
   quantity=1,
   unit="kg",
   source_text="商品行：苹果 1 kg，已收货",
   expires_at=null
   )
2. diet_pantry(
   action="add",
   food_name="鸡蛋",
   quantity=10,
   unit="枚",
   source_text="商品行：鸡蛋 10 枚，已收货",
   expires_at=null
   )
3. diet_pantry(
   action="add",
   food_name="牛奶",
   quantity=500,
   unit="ml",
   source_text="商品行：牛奶 250 ml × 2，已收货",
   package_count=2,
   quantity_per_package=250,
   package_unit="ml",
   expires_at=null
   )
4. diet_pantry(
   action="add",
   food_name="酸奶",
   quantity=200,
   unit="g",
   source_text="商品行：酸奶 200 g，已收货",
   expires_at=null
   )
5. diet_pantry(
   action="add",
   food_name="米",
   quantity=1,
   unit="kg",
   source_text="商品行：米 1 kg，已收货",
   expires_at=null
   )
6. diet_pantry(
   action="add",
   food_name="菠菜",
   quantity=300,
   unit="g",
   source_text="商品行：菠菜 300 g，已收货",
   expires_at=null
   )
7. diet_pantry(
   action="add",
   food_name="香蕉",
   quantity=500,
   unit="g",
   source_text="商品行：香蕉 500 g，已收货",
   expires_at=null
   )

不确定项目：
- 已折叠商品（第 1 件）；status=未知；原因：页面仅显示“另有2件商品已折叠，展开后查看”，名称、数量和状态不可见。
- 已折叠商品（第 2 件）；status=未知；原因：页面仅显示“另有2件商品已折叠，展开后查看”，名称、数量和状态不可见。

业务 trace（本回合）：
- expense_entry：未调用。
- diet_pantry：未调用。
- 预览状态：awaiting_confirmation。

是否确认执行？
可以回复：确认、只记账、只入库，或者直接说明修改内容。
```

Rationalization:

唯一直接对应的实付金额为 ¥168.00，七个食品行均明确已收货，因此费用投影可执行，混合超市订单使用公开分类 ID `shopping`；没有可靠订单时间时使用带时区的当前会话时间。七个已收货食品可作为 pantry 候选，牛奶的 250 ml × 2 可保留为包装三元组并计算为 500 ml。两件折叠商品的名称、数量和状态未知，必须逐件展示为不确定项目。首次图片回合仍然只能预览，不能实际调用写入工具。

Scenario C (corrected P03; evaluator identity: corrected_p03_evaluator (read-only current-materials evaluator)):

```text
该预览已消费。接下来不会再调用任何业务写入工具，也不会重试榴莲入库。

- `expense_entry(action="create")`：不再调用。支出已成功写入，状态为 `written`。
- `diet_pantry(action="add")`（榴莲）：不再调用。该项已发起过一次写入；工具已明确拒绝，状态为 `failed`。不会基于“时间很紧”改用、删去或猜测字段，也不会再次提交。
- 因此，接下来实际调用的字段：无新的业务调用，也无新的业务 payload。

已经发生的榴莲入库尝试只能是确认预览中对应的公开 `items[item_index]` payload：必填字段为 `action: "add"`、`food_name`、`quantity`、`unit`、`source_text`，以及预览中原本存在的、已声明的可选字段。`expires_at` 是允许的到期信息字段；`粒`是 `unit` 的值而不是字段名。现有材料不能据此判定应替换成哪个字段或值，因此不会构造修复后的 payload。

Execution result (this preview is consumed)
- Expense: committed.
- Pantry committed: none.
- Pantry not committed: 榴莲；写入被拒绝，原因为 `expires_at` 或 `粒`。
- Pantry indeterminate: none.
- Not submitted by scope: none.

若要修复，只能创建一个仅包含尚未提交榴莲的、面向用户可见的新修订预览；它不得重放已成功的支出，并且需要新的确认后才可提交。
```

Rationalization:

支出和 pantry 是独立业务域。支出已被下游确认成功，必须保留为 `written`，不得因 pantry 失败而回滚或重放。榴莲写入收到明确拒绝，属于已确认未提交的 `failed`，而非可自动重试的状态；原预览在执行开始时已被消费。当前材料既不授权猜测拒绝原因后修改字段，也不授权跳过新修订预览与新的确认。因此此刻没有任何新的业务调用，最终 trace 必须分别列出已提交的支出和未提交的榴莲。
## v2.1 + ledger v0.3 GREEN handoff comparison

This section is later GREEN evidence, not a revision of the v2.0.1 RED
baseline above. The fresh `task-8b-pressure-evaluation` independently checked
router revision `dc66a5e20b7370d7e2fd3eca53c7af84148ed4c5` with the ledger
v0.3 line-item worktree based on `dc18d3442815767ccbae257bafbb6a1f705fb648`.
An initial PowerShell UTF-8 read corrupted Chinese fixture text; bundled
Python `Path.read_text(encoding="utf-8")` plus `json.loads` corrected that
evaluator-only error. The corrected parse facts were one durian public line
item at `119.0` and seven partial-order public line items at `65.48`.

### Corrected Scenario B2 (verbatim)

**Decision**

PASS — issue exactly one order-level expense create using the confirmed partial
fixture’s public projection: 65.48, `shopping`, its timezone-bearing fixture
timestamp, `source_kind: "image"`, and all seven intact public line items.
Do not send null merchant/note or any Router internal fields.

**Exact tool calls**

```text
expense_entry.create({
  amount: 65.48,
  category_id: "shopping",
  occurred_at: "2026-08-05T00:00:00+08:00",
  source_kind: "image",
  line_items: <the exact seven-element UTF-8 fixture expense_projection.line_items array,
               anchored above by SHA-256 d9650b20412f81c2af3d01a9ab857031f99a8489ab0c594478be0d56423ec47a>
})
```

No second create and no per-product create is permitted.

**Default user reply**

```text
已记账 65.48 元，完整保留甜玉米、鲜牛奶等 7 种可见商品的数量、规格和实付；另有 2 种商品未展开，未把它们伪造成明细。
```

**Rationale**

The contracts require one expense per order, intact structured public
line-items, public-whitelist-only handoff, omission of optional nulls, and a
brief user receipt that retains the incomplete-detail warning.

### Corrected Scenario D (verbatim)

**Decision**

PASS — return the original indeterminate identity to Router orchestration; do
not create again and do not autonomously query, use CLI/shell, or write a
substitute entry.

**Exact tool calls**

```text
expense_entry.create: MUST NOT be called again.
expense_entry.search / expense_report / expense_system: MUST NOT be called autonomously.
CLI, shell, direct SQLite, and a substitute create: MUST NOT be used.
```

**Default user reply**

```text
这笔记录的提交结果暂时无法确认，我不会重复记账；目前也不能确认已经成功。
```

**Rationale**

The Ledger Skill explicitly forbids re-create, autonomous query/retry, CLI,
and shell after `indeterminate`; only Router orchestration can do its
documented query-first status recovery.
