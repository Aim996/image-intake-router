# 图片识别路由器不可变约束

1. **必须看真实像素：** 每张附件必须实际进入视觉能力；禁止无像素推断，文件名、附件描述和用户转述不能填充图片事实。
2. **有限识读：** 每张图完成一次初次识读；只有可见字段遗漏时允许最多一次补充识读，禁止第二次补充或无限重读。
3. **统一事实：** 清洗后的文字、识别字段、计算结果、来源、置信度和限制必须合并为一个 canonical fact set；预览与确认交接只能复用该事实集。
4. **不猜隐藏内容：** 折叠、遮挡、裁切、模糊、被阻止和不可读内容必须披露；禁止猜测隐藏商品、规格、数量、金额或退款，也不能用零或默认值伪装未知。
5. **可靠内容不被阻塞：** 存在隐藏或不可读项目时，可靠可见内容仍可预览；确认后只交接可靠可见内容并保留限制说明。
6. **固定详细预览：** 初始含图回合必须依次输出 `【入账内容】`、`【入库内容】`、`【需要注意】`，且图片回合零下游交接。
7. **后续一次确认：** 只有后续明确肯定确认才可交接；`只记账` 和 `只入库` 限定范围。禁止含糊确认、图片回合交接和重复交接。
8. **OpenClaw 所有权：** OpenClaw 负责下游 Skill 发现与调用；下游 Skill 负责业务校验、执行、存储、幂等、状态、重试与恢复。
9. **下游隔离：** 路由器不得检查或修改下游项目、私有 API、端口、接口、数据库或数据，不得维护私有 adapter manifest、payload 映射、版本协商或执行/状态协议。
10. **无数据迁移：** v3 不迁移、修改或删除下游数据，不要求下游仓库/API 变化。未来下游兼容性由 OpenClaw 或下游 Skill 处理。

公开契约见 [识图输出与确认规范](识图输出与确认规范.md)。
